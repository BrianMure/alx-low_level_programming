#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n = 97;
	for (; n <= 122 ; n++)
	{
		putchar(n);
	}
	putchar(10);
	return (0);
}
